name: Test Rule 1
meta:
  author: Niklas
definitions:
  ENUMLOL: 0
pattern:
  - or:
      foo:
        - x = foo(ENUMLOL)
      request:
        - x = HttpOpenRequestA(_, _, _, _, _, _, _)
  - InternetCloseHandle(x)"
